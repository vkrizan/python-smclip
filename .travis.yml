language: python

python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"

install:
  - pip install --editable .
  - pip install --upgrade pytest mock pytest-cov

script:
  - cd tests
  - py.test --verbose --cov=smclip
